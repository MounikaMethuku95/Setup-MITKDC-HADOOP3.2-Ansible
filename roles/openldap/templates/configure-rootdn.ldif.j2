# Configure rootDN for the main database (usually {2}hdb or {2}mdb)
dn: olcDatabase={{{ ldap_db_number }}}{{ ldap_db_type }},cn=config
changetype: modify
replace: olcRootDN
olcRootDN: {{ ldap_rootdn }}

-

replace: olcRootPW
olcRootPW: {{ ldap_hashed_pw }}
